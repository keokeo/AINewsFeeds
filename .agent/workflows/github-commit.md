---
description: 提交并推送代码到 GitHub
---
这是一个用于将当前项目的修改提交并推送到 GitHub 远程仓库的工作流。

1. 检查当前代码仓库的状态
`git status`

// turbo
2. 暂存所有已修改的文件和新文件
`git add .`

3. 提交更改（你可以在执行前修改双引号内的提交信息）
`git commit -m "Update project: refactored backend and frontend"`

// turbo
4. 推送代码到与其关联的远程仓库分支
`git push`
